{{#ember-wormhole to='admin-nav' }}
  {{#link-to 'admin.productos' class='breadcrumb' }}Productos{{/link-to}}
{{/ember-wormhole}}

<style>
	.row {
		margin-top: 3%;
	}

  body {
    background: url(https://images.unsplash.com/photo-1413745000559-46fdd2d81cd7?auto=format&fit=crop&w=1334&q=80);
  }

  .izq{
    margin-left: 3%;
  }

  .der{
    margin-right: 3%;
  }

  .spc{
    padding-right: 3% !important;
    padding-left: 3% !important;
  }

</style>

<div class="row">
  <div class="col s12">
  	{{#link-to "admin.nvo-receta" class="waves-effect waves-light btn left izq"}}<i class="material-icons left">add</i>Agregar Nueva Receta{{/link-to}}
    {{#link-to "admin.nvo-distribuido" class="waves-effect waves-light btn right der"}}<i class="material-icons left">add</i>Agregar Nuevo Distribuido{{/link-to}}
  </div>
  <div class="col s6 spc">
    <div class="card">
      <table class="centered">
        <thead>`
          <tr>
            <th>Nombre</th>
            <th>Precio</th>
            <th>Acciones</th>
          </tr>
        </thead>
        <tbody>
          
          {{#each myRecetas as |receta|}}
            {{#if receta.nombre}}
            <tr>
              <td>
                {{receta.nombre}}
              </td>
              <td>
                {{receta.precio}}
              </td>
              <td>
                <a class="btn-floating"><i class="material-icons" {{action 'editReceta' receta}}>edit</i></a>
                <a class="btn-floating"><i class="material-icons" {{action 'delete' receta}} >delete</i></a>
              </td>
            </tr>
            {{/if}}

          {{else}}
          <h4> Aún no tienes recetas.</h4>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>
    <div class="col s6 spc">
    <div class="card">
      <table class="centered">
        <thead>
          <tr>
            <th>Nombre</th>
            <th>Precio</th>
            <th>Acciones</th>
          </tr>
        </thead>
        <tbody>
          {{log myDistribuidos}}
          {{#each myDistribuidos as |exisDistribuido|}}
            {{#if exisDistribuido.producto.nombre}}
              <tr>
                <td>
                  {{distribuido.nombre}}
                </td>
                <td>
                  {{distribuido.precio}}
                </td>
                <td>
                  <a class="btn-floating"><i class="material-icons" {{action 'editDistribuido' distribuido}}>edit</i></a>
                  <a class="btn-floating"><i class="material-icons" {{action 'delete' distribuido}} >delete</i></a>
                </td>
              </tr>
            {{/if}}
            {{else}}
              <h4> Aún no tienes distribuidos.</h4>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>

</div>