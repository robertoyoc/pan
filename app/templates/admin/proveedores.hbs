{{#ember-wormhole to='admin-nav' }}
  {{#link-to 'admin.compras' class='breadcrumb' }}Compras{{/link-to}}
  {{#link-to 'admin.proveedores' class='breadcrumb' }}Proveedores{{/link-to}}
{{/ember-wormhole}}
<style type="text/css">
	body {
		background: url(https://images.unsplash.com/photo-1413745000559-46fdd2d81cd7?auto=format&fit=crop&w=1334&q=80) no-repeat fixed;
	}

	table {
		background: #FFF;
		margin-top: 4%;
	}

	.btn-floating {
		margin-left: 2%;
	}

  .waves-effect {
    margin-top: 3%;
    margin-bottom: 2%;
  }
 </style>
 <div class="row">
 	<div class="col s12 m8 offset-m2">
    {{#link-to "admin.nvo-proveedor" class="waves-effect waves-light btn right"}}<i class="material-icons left">add</i>Agregar Prooveedor{{/link-to}}
 		<table class="centered striped responsive-table">
        <thead>
          <tr>
              <th>Nombre</th>
              <th>Proxima Visita</th>
              <th>Opciones</th>
          </tr>
        </thead>

        <tbody>
        	{{#each proveedor}}
          <tr>
            <td>{{proveedor.nombre}}</td>
            <td>{{proveedor.fecha-visita}}</td>
            <td>{{#m-wrapper query="" method="" options=""}}
            	<a class="btn-floating waves-effect waves-light red tooltipped" data-position="bottom" data-delay="50" data-tooltip="Editar"><i class="material-icons">visibility</i></a>
            	<a class="btn-floating waves-effect waves-light red tooltipped" data-position="bottom" data-delay="50" data-tooltip="Ver"><i class="material-icons">edit</i></a>
            {{/m-wrapper}}</td>
          </tr>

          {{else}}
          <h4> Aún no tienes proveedores.</h4>
          {{/each}}
        </tbody>
      </table>
 	</div>
 </div>