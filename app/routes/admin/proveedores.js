import Route from '@ember/routing/route';

export default Route.extend({
    beforeModel(){
        /*
        this.store.createRecord('proveedor', {
		 	nombre: "Santa Clara",
         }).save()
         this.store.createRecord('proveedor', {
		 	nombre: "Gamesa",
         }).save()
        */
    }
});
