import Route from '@ember/routing/route';

export default Route.extend({

	beforeModel(){
		// this.store.createRecord('mprima', {
		// 	nombre: 'undefined'
		// }).save()
		// this.store.createRecord('distribuido', {
		// 	nombre: 'undefined'
		// }).save()
		// this.store.createRecord('receta', {
		// 	nombre: 'undefined'
		// }).save()
	}

});
